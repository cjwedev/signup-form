name: Deploy website on push
on: 
  push:
    branches:
    - live
  workflow_dispatch:
jobs:
  web-deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
    - name: Get latest code
      uses: actions/checkout@v2
      with: 
        ref: live

    - name: Use Node.js 14
      uses: actions/setup-node@v2
      with:
        node-version: '14'
      
    - name: Build Frontend
      run: |
        cd frontend
        npm install
        npm run build
    
    - name: Sync files
      uses: SamKirkland/FTP-Deploy-Action@3.1.1
      with:
        local-dir: frontend/build/
        ftp-server: sftp://92.63.169.233
        ftp-username: github
        ftp-password: ${{ secrets.ftp_password }}
        entrypoint: var_www/signup/frontend/
        git-ftp-args: --insecure
      
    - name: Build Backend
      run: |
        cd /home/runner/work/SignUp/SignUp/backend
        dotnet publish --configuration Release -r "linux-x64"
    
    - name: Sync files
      uses: SamKirkland/FTP-Deploy-Action@3.1.1
      with:
        local-dir: backend/
        ftp-server: sftp://92.63.169.233
        ftp-username: github
        ftp-password: ${{ secrets.ftp_password }}
        entrypoint: var_www/signup/backend/
        git-ftp-args: --insecure
